# 实验记录本

本文件与 `output/experiment_log.csv` 同步维护，记录每轮实验的操作、结果与分析结论。

---

## 实验 1 — 基线版本

| 项目 | 值 |
|---|---|
| 时间 | 2026-07-11 16:08:49 |
| CV-RMSE | **173.67** |
| 线上得分 | **278.50** |
| 处理 | 未修改（原始 baseline） |

**α 网格:** `[1.0, 10.0, 50.0, 100.0, 500.0, 1000.0, 5000.0, 10000.0]`

**结论:** 基线得分，作为后续实验的参照锚点。

---

## 实验 2 — Ridge α 网格改为 3x 等比序列

| 项目 | 值 |
|---|---|
| 时间 | 2026-07-11 17:05:11 |
| CV-RMSE | **169.87** ↓（较基线降 3.8） |
| 线上得分 | **286.71** ↑（较基线升 8.2，更差） |
| 处理 | config.py 中 Ridge 网格搜索范围修改 |

**α 网格:** `[1.0, 3.0, 10.0, 30.0, 100.0, 300.0, 1000.0, 3000.0, 10000.0]`

**分析:**

- CV-RMSE 下降是好事，但线上得分反而上升（变差），说明**线下 CV 与线上评测不一致**。
- 推测原因: 小样本（每煤种 7~27 批）下 GroupKFold 的 splits 不稳定，RidgeCV 选择的 α 过拟合到了特定 CV 折的噪声上，未能泛化到真实测试集。
- RidgeCV 候选值越多，过拟合 CV 折的风险越大。

---

## 实验 3 — Ridge α 网格改为前密后疏

| 项目 | 值 |
|---|---|
| 时间 | 2026-07-11 17:11:06 |
| CV-RMSE | **178.16** ↑（较基线升 4.5） |
| 线上得分 | —（未提交） |
| 处理 | 看到上一轮最优 α 多为 1 或 3，因此把低区加密为 1/2/3/5/10 |

**α 网格:** `[1.0, 2.0, 3.0, 5.0, 10.0, 30.0, 100.0, 300.0, 1000.0, 3000.0, 10000.0]`

**分析:**

- CV-RMSE 比基线还差，说明低区过度加密让 RidgeCV 选到了局部噪声最优值。
- 结合实验 #2 的结果，进一步印证: **在小样本场景下，RidgeCV 的 α 候选集不宜过密**，否则容易选中过拟合的 α。

---

#---

## 实验 4 — 根据物理谱线和峰位修正 KEY_LINES + 按批次数加权 CV-RMSE

| 项目 | 值 |
|---|---|
| 时间 | 2026-07-11 20:06 (实验) / 2026-07-11 23:14 (加权修正) |
| CV-RMSE | **174.14**（按批次数加权，原简单平均 174.92） |
| 线上得分 | **285.77** ↑（较基线升 7.3，更差） |
| 处理 | 遍历实际光谱数据修正 KEY_LINES（中心波长对齐实测峰位、半窗口缩小到 FWHM×3~5、移除 SNR=0.16 的 Fe），同时将全局 CV-RMSE 从算数平均改为按批次数加权平均 |

**修改内容:**

**KEY_LINES 修正:**
- 中心波长对齐实测峰位：C 247.86→247.93, H 656.3→656.96, O 777.2→777.68, Ca 422.7→422.87, Ca2 393.4→393.73, Mg 285.2→285.39, Al 308.2→309.35, Si 288.2→288.35, Na 589.0→589.56
- 半窗口缩小到物理 FWHM 的 3-5 倍（窄线 0.6-0.8 nm，H 因 Stark 展宽保持 3.5 nm）
- 移除 Fe（SNR=0.16，纯噪声）
- 新增`src/window_search.py` 窗口自动搜索工具

**全局 CV-RMSE 计算方式变更:**
- 原: 各煤种 Stage2 CV-RMSE 的算数平均
- 现: 按批次数加权平均（批次数多的煤种贡献更大的权重，其 CV 估计更可靠）

**加权方式验证:**
验证了 3 组历史 experiment（baseline / Ridge grid / KEY_LINES），按批次数加权后本地 CV-RMSE 与线上分数的变化方向实现全协同。详见 `src/weight_analysis.py`。

**结论:**
- KEY_LINES 修正本身（中心波长 + 缩窄窗口）没有提升线上分数
- 按批次数加权的 CV-RMSE 计算方式保留，因为它更好地反映了真实泛化性能
- KEY_LINES 修正回滚到原始值

---

# 当前结论

1. **基线网格 `[1, 10, 50, 100, 500, 1000, 5000, 10000]` 线上表现最佳**，虽然 CV-RMSE 不是最低，但泛化性最好。
2. 小样本（≤30 批）场景下，RidgeCV 的候选 α 不宜过密，否则 α 选择本身会成为过拟合源。
3. 后续调参应**优先在特征工程或模型结构上寻找改进方向**，而非在正则化网格上过度雕琢。

---

## 实验 5-7 — LightGBM 替代 RidgeCV 对比试验

| 实验 | 时间 | CV-RMSE | 处理 |
|---|---|---|---|
| #5 | 2026-07-12 11:25 | **251.99** ↑ | Stage1 → LGBMRegressor(500 trees), Stage2 保持 RidgeCV |
| #6 | 2026-07-12 11:26 | **265.24** ↑ | Stage1 保持 RidgeCV, Stage2 → LGBMRegressor(500 trees) |
| #7 | 2026-07-12 11:26 | **253.63** ↑ | 两阶段均替换为 LGBMRegressor(500 trees) |

**各煤种 Stage2 CV-RMSE 明细:**

| 煤种 | 基线(Ridge) | #5 Stage1 LGBM | #6 Stage2 LGBM | #7 Both LGBM |
|---|---|---|---|---|
| 赵固一矿 | ~174 | 278 | 393 | 304 |
| 赵固二矿 | ~150 | 153(sh=0) | 127(w=1) | 153(sh=0) |
| 中马矿 | ~287 | 341(w=1) | 208(w=1) | 260(w=1) |
| 九里山矿 | ~192 | 192 | 210 | 196 |
| 煤场混煤 | ~284 | 284 | 301 | 296 |
| **全局加权** | **172.66** | **251.99** | **265.24** | **253.63** |

**分析:**
- 所有 LGBM 变体均显著差于 RidgeCV 基线（全局 CV-RMSE 从 172 升至 250+）
- 核心原因：小样本（每煤种 7-27 批次 / 86-360 条光谱）下，500 棵树的 LGBM 严重过拟合，尤其赵固一矿（393 → 304 → 278，远差于基线）
- 赵固二矿（7 批）的 w=0 收缩完全否定了模型预测，说明模型本身预测不可靠
- **结论：LGBM 不适合当前小样本场景，RidgeCV 的强正则化更适合本任务**

**后续方向:**
- 如需引入树模型，应大幅限制树复杂度（n_estimators ≤ 100, max_depth ≤ 3, min_child_samples ≥ 10）
- 或考虑 XGBoost（已在依赖中）配合更高正则化参数

---

## 实验 8-13 — 小树模型对比试验 (n_estimators=50, max_depth=2)

**参数设计:**
- LGBM-small: `num_leaves=7, min_child_samples=10, subsample=0.8, colsample_bytree=0.8, reg_alpha=0.5, reg_lambda=0.5`
- XGBoost: `learning_rate=0.3, subsample=0.8, colsample_bytree=0.8, reg_alpha=1.0, reg_lambda=1.0`

| 实验 | 模型 | 修改范围 | CV-RMSE | vs 基线 |
|---|---|---|---|---|
| #8 | LGBM-small | Stage1 only | **226.92** | +54 |
| #9 | LGBM-small | Stage2 only | **277.71** | +105 |
| #10 | LGBM-small | Both | **273.75** | +101 |
| **#11** | **XGBoost** | **Stage1 only** | **208.33** ⭐ | **+36 最佳** |
| #12 | XGBoost | Stage2 only | **272.89** | +100 |
| #13 | XGBoost | Both | **250.97** | +78 |

**各煤种 Stage2 CV-RMSE 明细（最优变体 #11 vs 基线）:**

| 煤种 | 基线(Ridge) | #8 LGBM-s1 | #11 XGB-s1 ⭐ |
|---|---|---|---|
| 赵固一矿 | ~174 | 256 | **237** |
| 赵固二矿 | ~150 | 104(sh=1) | **34(sh=1)** |
| 中马矿 | ~287 | 287(w=1) | **253(w=1)** |
| 九里山矿 | ~192 | 182 | **176** |
| 煤场混煤 | ~284 | 261 | **252** |
| **全局加权** | **172.66** | **226.92** | **208.33** |

**关键发现:**

1. **Stage1 替换比 Stage2 替换更有希望** — 在所有 6 个变体中，替换 Stage1 的 CV-RMSE 总是低于替换 Stage2 的
   - 原因: Stage1 有更多训练样本（每条光谱一个样本），而 Stage2 只有批次级别的样本（7-27 个）
   
2. **缩小树规模明显改善** — 从 #5 (500 trees) 的 251.99 降到 #8 (50 trees) 的 226.92，再到 #11 (XGB) 的 208.33

3. **XGBoost Stage1 only (#11) 是所有树变体中最佳的** — 208.33，虽然仍比 RidgeCV 基线 172.66 差 36 点

4. **Stage2 不适合任何树模型** — 只要 Stage2 换成树模型（#9, #10, #12, #13），CV-RMSE 全部跳到 250+，说明 7-27 个批次样本根本不够训练树模型

---

## 实验 14-15 — 树模型 + 正则化强度搜索 (Stage1 only)

**搜索空间:** 10 组 (reg_alpha, reg_lambda) 候选对，范围 0.5 ~ 100，每组都做完整 CV 选最优

| 实验 | 模型 | CV-RMSE | vs 基线 | 备注 |
|---|---|---|---|---|
| 基线 | RidgeCV | **172.66** | — | |
| #8 | LGBM-small (50t,d2) 无搜索 | 226.92 | +54 | 固定 (0.5, 0.5) |
| #11 | XGBoost (50t,d2) 无搜索 | 208.33 | +36 | 固定 (1.0, 1.0) |
| **#15** | **LGBM + reg-search** | **188.08** ⭐ | **+15** | 各煤种自适应正则 |
| #14 | XGBoost + reg-search | 191.36 | +19 | 各煤种自适应正则 |

**各煤种最佳正则参数:**

| 煤种 | LGBM 最佳 (α_l1, α_l2) | XGBoost 最佳 (α_l1, α_l2) |
|---|---|---|
| 赵固一矿 (11批) | (5.0, 0.5) → 208 | (50.0, 50.0) → 193 |
| 赵固二矿 (7批) | (10.0, 10.0) → 23 | (10.0, 10.0) → 26 |
| 中马矿 (7批) | — | — |
| 九里山矿 (18批) | — | — |
| 煤场混煤 (27批) | — | — |

**关键发现:**
1. **正则化搜索带来巨大提升** — LGBM 从 226.92 → 188.08（-39 点），XGBoost 从 208.33 → 191.36（-17 点）
2. **小样本煤种需要极强正则** — 赵固一矿（11批）最佳正则高达 (50, 50)，而赵固二矿（7批）需要 (10, 10)
3. **LGBM 略优于 XGBoost** — 188.08 vs 191.36，差距很小
4. **RidgeCV 基线 172.66 仍未被超越**，但差距已缩小到 15 点

**最终结论:**
- LGBM Stage1 + 正则搜索 + RidgeCV Stage2 是树模型混搭最佳方案（188.08）
- 但 RidgeCV 纯线性模型（172.66）仍然更优 — 说明 LIBS 光谱本质上是线性/弱非线性问题，树模型的非线性能力在小样本下反而有害
- **推荐方向**: 回到特征工程/数据增强，或切换回原始 CV 计算方式

---

## 实验 16 — 原始 CV 计算方式对比（简单平均 vs 加权平均）

**背景:** 实验 #4 把全局 CV-RMSE 从简单平均改成了按批次数加权平均。
实验 #1 基线（173.67）是用简单平均算的。此处重现 LGBM reg-search 在原始计算方式下的值。

| 实验 | 模型 | CV 计算方式 | CV-RMSE | vs RidgeCV 同方法 |
|---|---|---|---|---|
| #1 | RidgeCV | 简单平均 | **173.67** | — |
| #15 | LGBM reg-search | 加权平均 | 188.08 | +15（差） |
| **#16** | **LGBM reg-search** | **简单平均** | **150.80** 🏆 | **−22.87（胜）** |

**分析:**
- 在原始简单平均下，LGBM Stage1 reg-search + RidgeCV Stage2 **首次超越 RidgeCV 基线**！
- 原因: 简单平均给每个煤种等权重。小煤种（赵固二矿、中马矿，各 7 批）树模型表现极好（CV ~23-30），拉低了全局均值
- 加权平均下，大煤种（煤场混煤 27 批，九里山矿 18 批）权重更大，而它们的 CV-RMSE 较高（~250-260），掩盖了小煤种的提升

**关键启示:**
- **LGBM + 正则搜索 + RidgeCV Stage2 在简单平均下是当前最佳方案（150.80）**
- 加权平均更"诚实"（大煤种的 CV 估计更可靠），但简单平均是原始赛题评估方式
- 🚨 **需要明确: 赛题最终评分用的是哪种聚合方式？** 若按批次加权，则 RidgeCV 仍最优；若各批次等权重，则 LGBM 混合模型完胜

---

## 实验 17 — 预测值异常值剔除（Outlier Removal）

**动机:** 单批次内多条光谱的预测值可能存在离群，先剔除再取中位数或许比直接中位数更鲁棒。

**处理:** 在 `aggregate_to_batch` 中加入 `_remove_outliers` 辅助函数，支持 4 种剔除方法。训练 OOF 聚合和推理阶段都应用同一套逻辑。代码保留在 `feat/outlier-removal` 分支（`LIBS_outlier_removal` worktree），不合并主分支。

**实验分组:**

| 场景 | 方法 | 全局 CV-RMSE | vs 基线 | 线上得分 |
|---|---|---|---|---|
| 基线 | none（不剔除） | **172.66** | — | **278.50** |
| 全部剔除 | zscore-2 | 173.42 | +0.76 ❌ | — |
| 全部剔除 | zscore-1.5 | 173.05 | +0.39 ❌ | — |
| 全部剔除 | modified-zscore | 173.25 | +0.59 ❌ | — |
| 全部剔除 | iqr | 173.49 | +0.83 ❌ | — |
| **仅赵固一矿** | **zscore-2** | **171.54** | **−1.12** ✅ | **284.81** ❌ |
| 仅赵固一矿 | zscore-1.5 | 172.30 | −0.36 | — |
| 仅赵固一矿 | modified-zscore | 172.50 | −0.16 | — |
| 仅赵固一矿 | iqr | 172.08 | −0.58 | — |

**关键发现:**
1. **全部剔除 → 全局恶化**（173+），其他煤种剔除丢掉信息
2. **仅赵固一矿 zscore-2** 是 CV 上最好的（全局 171.54，↓1.12），但线上 **284.81** 比基线 **278.50** 差
3. CV-RMSE 下降是 **CV 乐观偏差**：训练时模型对未见数据预测方差大，剔除"错误预测"有效；推理时全量模型预测高度一致，剔除反而丢信息

**最终结论:** ❌ **已放弃。** 预测值层面的异常值剔除本质是掩盖模型不确定性，而非修复光谱质量问题。

---

## 实验 18 — 分煤种正则化窗口（Config B）

**背景：** 各煤种批次差异大（7-27批），试图对不同数据量级煤种设置不同的 Ridge alpha 搜索窗口。
大批次（≥11批）用弱正则化 WEAK=[0.1,0.3,0.9]，小批次（≤7批）用强正则化 STRONG=[3,10,30,100,300,1000]。

**结果：**

| 项目 | 值 |
|---|---|
| 时间 | 2026-07-13 22:27 |
| 全局 CV-RMSE | **163.37** |
| 线上得分 | **281.97** ❌ |

| 煤种 | 策略 | α候选池 | 最优α | CV-RMSE |
|---|---|---|---|---|
| 赵固一矿豫焦末煤(11批) | WEAK | [0.1,0.3,0.9] | 0.3 | 144.78 |
| 赵固二矿中煤矿(7批) | STRONG | [3,10,30,100,300,1000] | 3.0 | 117.31* |
| 中马矿中煤矿(7批) | STRONG | [3,10,30,100,300,1000] | 3.0 | 220.61* |
| 九里山矿中煤矿(18批) | WEAK | [0.1,0.3,0.9] | 0.3 | 141.24 |
| 煤场混煤(27批) | WEAK | [0.1,0.3,0.9] | 0.1 | 182.80 |

**分析：**
- 三个大批次煤种全选最弱 α（0.1/0.3），几乎等于无正则化的 OLS→严重过拟合
- CV-RMSE 虚低（163.37 vs 基线172.66），但线上 281.97 远差于基线 278.50
- **结论：α < 1 导致严重过拟合，分煤种调整正则化在此场景不奏效**

---

## 实验 19 — 前疏后密分煤种正则化（D1）

**背景：** 从实验 #18 吸取教训，WEAK 窗口从 α=1 起步（去掉 <1），STRONG 从 α=3 起步。
采用"前疏后密"原则：WEAK=[1,3,10,15,20,25,30,60,80,100]，STRONG=[3,10,15,20,25,30,60,80,100]。

**结果：**

| 项目 | 值 |
|---|---|
| 时间 | 2026-07-13 22:45 |
| 全局 CV-RMSE | **172.04** |
| 线上得分 | **287.87** ❌ |

| 煤种 | 策略 | α候选池 | 最优α | CV-RMSE |
|---|---|---|---|---|
| 赵固一矿豫焦末煤(11批) | WEAK | [1,3,10,15,20,25,30,60,80,100] | 1.0 | 174.87 |
| 赵固二矿中煤矿(7批) | STRONG | [3,10,15,20,25,30,60,80,100] | 3.0 | 116.31* |
| 中马矿中煤矿(7批) | STRONG | [3,10,15,20,25,30,60,80,100] | 3.0 | 203.71* |
| 九里山矿中煤矿(18批) | WEAK | [1,3,10,15,20,25,30,60,80,100] | 1.0 | 146.82 |
| 煤场混煤(27批) | WEAK | [1,3,10,15,20,25,30,60,80,100] | 1.0 | 193.94 |

**分析：**
- WEAK 全选 α=1（最小值），STRONG 全选 α=3（最小值）— **RidgeCV 总是选池子里最小的 α**
- CV-RMSE 172.04 与基线 172.66 几乎持平，但线上 287.87 反而更差
- 说明 STRONG 类型从 α=1→3 是**过度正则化**，小煤种（7批/86-96光谱）也适合 α=1
- 额外候选值（15,20,25,30,...）完全没有被选中，纯属冗余

**最终结论：**
1. **所有煤种最优 α 都在 1 附近**，分煤种定制 α 窗口没有带来提升
2. α < 1 → 严重过拟合（#18）
3. α > 1 对小煤种 → 欠拟合（#19）
4. **原始全局 ALPHAS=[1,10,50,100,500,1000,5000,10000] 仍是线上最优（278.50）**
5. 后续优化应转向特征工程或模型结构，而非继续雕琢正则化网格
